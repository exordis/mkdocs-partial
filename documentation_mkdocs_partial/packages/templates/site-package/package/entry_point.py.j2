import inspect
import os

from {{module_name}} import __version__
from documentation_mkdocs_partial.site_entry_point import SiteEntryPoint

def run():
    script_dir = os.path.dirname(__file__)
    SiteEntryPoint(__version__, os.path.join(script_dir, "site")).run()
